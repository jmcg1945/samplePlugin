function.setlogleveldebug.description=Set the log level for the specified class
function.setlogleveldebug.param.className.description=Name of the class to configure (don't include ".class")

function.setloglevelwarn.description=Set the log level for the specified class
function.setloglevelwarn.param.className.description=Name of the class to configure (don't include ".class")

function.setloglevelinfo.description=Set the log level for the specified class
function.setloglevelinfo.param.className.description=Name of the class to configure (don't include ".class")

function.setloglevelerror.description=Set the log level for the specified class
function.setloglevelerror.param.className.description=Name of the class to configure (don't include ".class")

function.getloglevel.description=Get the current log level for the specified class
function.getloglevel.param.className.description=Name of the class (don't include ".class")